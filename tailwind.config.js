/**
 * Tailwind v4 reads design tokens from `src/index.css` under `@theme`
 * (bg, ink, terracotta, cobalt, sun, muted, card + Syne/Inter font families).
 * This config is kept minimal and explicit for tooling.
 */
export default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
}
